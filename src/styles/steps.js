import {colors} from '../styles/colors'

export const Steps_ = (Arrgcolor) => {
    return {
        backgroundColor : colors[Arrgcolor] ,
        height : '2px',
        with : '35px',
        margin : '0 0.2em 0 0.2em',
        borderRadius:'1em',
    }
}