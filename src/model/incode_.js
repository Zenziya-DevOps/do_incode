import { Key } from './Key'

export const Incode_ = {
    mp_id : new URLSearchParams(window.location.search).get("sidcs"),
    KeyOrigen : Key.KeyOrigen,
    Inc_InterviewId: null,
    Inc_InterivewCode : null,
    Inc_Token : null,
    Inc_Initial : true
}